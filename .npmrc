
# NPM Configuration for EAS Build
# Force legacy peer dependencies to avoid conflicts
legacy-peer-deps=true

# Disable package hoisting to ensure correct dependency resolution
hoist=false

# Use exact versions
save-exact=true

# Increase timeout for slow connections
fetch-timeout=600000
